<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details </title>
    <!-- Include Tailwind CSS via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h2><a href="/products">back</a></h2>
        <h1 class="text-3xl font-bold mb-6">Product Details</h1>
        <section class="w-100%" flex >
            <div class="bg-white w-[50%] rounded-lg shadow-lg overflow-hidden flex flex-col justify-center items-center">
                <img src="<%= product.img %>" alt="Product Image" class="w-full h-64 object-contain object-center">
                <div class="p-6">
                    <h2 class="text-2xl font-semibold mb-4"><%= product.name %></h2>
                    <p class="text-gray-700 mb-4"><%= product.desc %></p>
                    <div class="flex items-center mb-4">
                        <span class="text-lg font-bold text-green-500 mr-2">$<%= product.price %></span>
                        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Add to Cart</button>
                    </div>
                    <div class="flex items-center">
                        <span class="text-gray-600 mr-2">Category:</span>
                        <span class="text-gray-800 font-semibold">Electronics</span>
                    </div>
                </div>
                <h3>Reviews</h3>
                <ul>
                     <li></li>
                </ul>
            </div>
            <div>
                <h2>Add Review</h2>
                <form action="/products/<%= product._id %>/rating" method="post">
                    <label for="rating">Rating:</label>
                    <input type="range" name="rating" id="rating" min="0" max="5">
                    <br>
                    <label for="comment">Comment:</label>
                    <textarea name="comment" id="comment" cols="20" >
                    </textarea>
                    <input type="submit" value="submit">
                </form>
            </div>

        </section>
        
    </div>
</body>

</html>
