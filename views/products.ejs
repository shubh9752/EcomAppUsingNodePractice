<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Products</title>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6">Product Cards </h1>

        <% for(let items of products ){ %>

        <div class="flex flex-wrap justify-center -mx-4">
            <!-- Card 1 -->
            <div class="bg-white w-[20%] h-[20%] rounded-lg shadow-lg overflow-hidden ">
                <img src="<%= items.img %>" alt="Product Image" class="w-full h-40 object-cover object-center">
                <div class="p-4">
                    <h2 class="text-xl font-semibold mb-2"><%= items.name %></h2>
                    <p class="text-gray-700 mb-4"><%= items.desc %></p>
                    <div class="flex flex-wrap justify-between items-center">
                        <span class="text-lg font-bold text-green-500">$<%= items.price %></span>
                        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Buy Now</button>
                        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"><a href="/products/<%= items._id %>">Show Details</a></button>
                        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"><a href="/products/<%= items._id %>/edit">Edit Details</a></button>
                    </div>
                    <form action="/products/<%= items._id %>?_method=DELETE" method="POST">
                         <button type="submit" class="bg-red-200 text-white h:bg-red-600">Delete</button>
                    </form>
                </div>
            </div>
        </div>
        <% } %> 
    </div>
    
</body>
</html>